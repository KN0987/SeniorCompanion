# SeniorCompanion

# Run React Native App
In "root project" run:

```
npm install (if not installed dependency already)
```

```
npm run dev
```

# Run Server:

In "/server" run:

```
npm install (if not installed dependency already)
```

```
npm start
```
